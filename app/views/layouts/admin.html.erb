<!DOCTYPE html>
<html>
  <head>
    <title>Nordik Admin</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body class="bg-gray-100 font-sans text-black-800">
    <div class="min-h-screen flex flex-col">
      <header class="bg-nordik-primary text-black py-4 px-8 flex justify-between items-center shadow-md">
        <div class="font-bold tracking-widest uppercase text-lg">Nordik Dashboard</div>
        <%= link_to "View Site →", root_path, class: "text-xs uppercase tracking-widest hover:text-nordik-accent transition", target: "_blank" %>
      </header>

      <main class="flex-grow container mx-auto p-8">
        <% if notice %>
          <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative mb-6" role="alert">
            <span class="block sm:inline"><%= notice %></span>
          </div>
        <% end %>

        <%= yield %>
      </main>
    </div>
  </body>
</html>